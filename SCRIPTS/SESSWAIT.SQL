SELECT
--C.SERIAL#,
--A.SID,
--C.USER#,
--C.USERNAME,
--C.LOCKWAIT,
C.OSUSER,
--C.MACHINE,
A.EVENT,
A.TOTAL_WAITS,
--A.TOTAL_TIMEOUTS,
A.TIME_WAITED,
--A.AVERAGE_WAIT,
--A.MAX_WAIT,
B. WAIT_TIME,
B.SECONDS_IN_WAIT WIS
FROM
V$SESSION_EVENT A,
V$SESSION_WAIT B,
V$SESSION C
WHERE A.SID=B.SID
AND A.SID=C.SID
AND C.USERNAME IS NOT NULL
AND A.TIME_WAITED > 0
ORDER BY B.SECONDS_IN_WAIT DESC
/
